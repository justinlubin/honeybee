1. Bump version numbers for editor, engine, honey_lang, and honey_libs
2. Update CHANGELOG section
3. Push commit
4. Add version tag: `git tag -a vX.Y.Z -m "Version X.Y.Z"`
5. Push version tag `git push origin vX.Y.Z`
6. Run `./publish.sh new-version` in `editor`
